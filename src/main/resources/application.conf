admin {
  profile = "slick.jdbc.PostgresProfile$"
  db = {
    driver = org.postgresql.Driver
    url = "jdbc:postgresql://localhost:5432/aergo"
    schema = "aergo"
    user = "postgres"
    password = "admin123"
    url = ${?POSTGRES_ADMIN_URL}
    user = ${?POSTGRES_ADMIN_USER}
    password = ${?POSTGRES_ADMIN_PASSWORD}
  }
}